apiVersion: v1
kind: Service
metadata:
  name: hospital-service
  namespace: hospital
spec:
  selector:
    app: hospital-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer